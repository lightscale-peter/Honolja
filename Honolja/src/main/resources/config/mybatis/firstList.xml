<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="firstlist">

	<select id="selectAll"  resultType="com.guest.honolja.list.ListDTO">
		select * from ( select rownum rn, g_no, g_name, g_addr, g_position_n, g_detail, g_url, g_position_e 
		from(select * from guesthouse where g_name like '%'|| #{sval} || '%')
 	   )
	</select> 
	
	<select id="countAll" resultType="int">
		select count(*) from guesthouse
	</select>

	<select id="selectlo"  resultType="com.guest.honolja.list.ListDTO">
		select rownum rn, g.* from guesthouse g 
		where g_addr like '%제주%'
	</select>
	
	<select id="countAll" parameterType="com.guest.honolja.list.ListDTO" resultType="int">
		select count(*) from guesthouse where g_name like '%' || #{sval} || '%'
	</select>

</mapper>